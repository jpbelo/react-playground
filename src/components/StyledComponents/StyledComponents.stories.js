import React from 'react';
import { storiesOf } from '@storybook/react';
import StyledComponents from '.';

storiesOf('Components|StyledComponents', module).add('with text', () => (
  <StyledComponents/>
));
